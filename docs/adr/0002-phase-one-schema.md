# ADR 0002: Phase 1 MongoDB schema

Because the referenced PROJECT_GUIDE database design is absent, Phase 1 uses explicit collection names, immutable audit entries, TTL leases, unique identity keys, and compound access-query indexes documented in the Mongoose schemas.
